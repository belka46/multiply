module.exports = function multiply(first, second) {
  let numFirst = Number(first);
  let numSecond = Number(second);
  return String(numFirst * numSecond);
}

/*function mult(str1, str2) {
    let numFirst = Number(str1);
    let numSecond = Number(str2);
    let res = numFirst * numSecond;
    toString(res);
    alert(res)
    return numFirst * numSecond;
}

mult('5', '10');*/